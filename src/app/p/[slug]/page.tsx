export const dynamic="force-dynamic";export const runtime="nodejs";import {db} from '@/lib/db';import {notFound} from 'next/navigation';import {ProductDetailPage} from '@/components/public/product-detail-page';export default async function ProductBySlug({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const stone=await db.stone.findFirst({where:{OR:[{slug},{code:slug}]},select:{id:true}});if(!stone)notFound();return <ProductDetailPage initialProductId={stone.id}/>}
