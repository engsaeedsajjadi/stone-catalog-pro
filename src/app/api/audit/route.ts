export const runtime="nodejs";export const dynamic="force-dynamic";import {NextRequest,NextResponse} from 'next/server';import {db} from '@/lib/db';import {requireAuth} from '@/lib/auth';export async function GET(req:NextRequest){const a=await requireAuth(req,['ADMIN']);if('response'in a)return a.response;const take=Math.min(Number(req.nextUrl.searchParams.get('take')||100),500);const rows=await db.activityLog.findMany({take,orderBy:{createdAt:'desc'},include:{user:{select:{name:true,email:true}}}});return NextResponse.json({success:true,data:rows})}
