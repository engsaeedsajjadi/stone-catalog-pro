export const runtime="nodejs";export const dynamic="force-dynamic";import {NextRequest,NextResponse} from 'next/server';import {db} from '@/lib/db';import {requireAuth} from '@/lib/auth';export async function GET(){return NextResponse.json({success:true,data:await db.salesStage.findMany({orderBy:{order:'asc'}})})}export async function POST(req:NextRequest){const a=await requireAuth(req,['ADMIN','SALES_MANAGER']);if('response'in a)return a.response;try{const b=await req.json();return NextResponse.json({success:true,data:await db.salesStage.create({data:{name:String(b.name),color:b.color,order:Number(b.order||0),isWon:Boolean(b.isWon),isLost:Boolean(b.isLost)}})},{status:201})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'Failed'},{status:400})}}
