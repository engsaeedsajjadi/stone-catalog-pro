export const runtime="nodejs";export const dynamic="force-dynamic";import {NextRequest,NextResponse} from 'next/server';import {db} from '@/lib/db';import {hashToken,issueRefreshToken} from '@/lib/security';import {sendNotification} from '@/lib/notifications';export async function POST(req:NextRequest){try{const {email}=await req.json();const user=await db.user.findUnique({where:{email:String(email||'').trim().toLowerCase()}});if(!user)return NextResponse.json({success:true});const token=issueRefreshToken();await db.passwordResetToken.create({data:{userId:user.id,tokenHash:hashToken(token),expiresAt:new Date(Date.now()+30*60*1000)}});const base=process.env.NEXT_PUBLIC_APP_URL;if(!base)throw new Error('NEXT_PUBLIC_APP_URL is not configured');await sendNotification('EMAIL',user.email,'بازیابی رمز عبور',`${base}/reset-password?token=${encodeURIComponent(token)}`);return NextResponse.json({success:true})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'Password reset provider is not configured'},{status:503})}}
