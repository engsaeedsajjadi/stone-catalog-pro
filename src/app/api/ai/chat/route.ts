export const runtime="nodejs";export const dynamic="force-dynamic";import {NextRequest,NextResponse} from 'next/server';import {aiChat} from '@/lib/ai';import {z} from 'zod';const s=z.object({messages:z.array(z.object({role:z.enum(['system','user','assistant']),content:z.any()})).min(1)});export async function POST(req:NextRequest){try{return NextResponse.json({success:true,data:{reply:await aiChat(s.parse(await req.json()).messages)}})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'AI is not configured'},{status:503})}}
