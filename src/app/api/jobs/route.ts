export const runtime="nodejs";export const dynamic="force-dynamic";import {NextRequest,NextResponse} from 'next/server';import {db} from '@/lib/db';import {requireAuth} from '@/lib/auth';export async function GET(req:NextRequest){const a=await requireAuth(req,['ADMIN']);if('response'in a)return a.response;return NextResponse.json({success:true,data:await db.job.findMany({take:100,orderBy:{createdAt:'desc'}})})}export async function POST(req:NextRequest){const a=await requireAuth(req,['ADMIN']);if('response'in a)return a.response;const b=await req.json();return NextResponse.json({success:true,data:await db.job.create({data:{type:String(b.type),payload:JSON.stringify(b.payload||{}),availableAt:b.availableAt?new Date(b.availableAt):new Date()}})},{status:201})}
